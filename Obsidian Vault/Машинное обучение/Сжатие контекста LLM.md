
## Сжатие входов (Input Compression)
#### 🔁 **Рекуррентные подходы**

- **[RMT](https://chatgpt.com?q=RMT), [AutoCompressors](https://chatgpt.com?q=AutoCompressors)**:
    
    - Делят длинный ввод на сегменты
        
    - Каждый сегмент → **вектор-саммари**
        
    - Саммари передаются рекурсивно
        
    - Используется одна и та же LLM
        

#### 🧠 **ICAE (2024)**

- Использует **автоэнкодерную архитектуру**
    
- **Энкодер** = та же LLM с **LoRA**-адаптацией
    
- **Декодер** = замороженная LLM
    
- Обучается на задачах:
    
    - автоэнкодинга
        
    - языкового моделирования
        
**[LoRA (Low-Rank Adaptation)](https://chatgpt.com?q=LoRA%20\(Low-Rank%20Adaptation\))** — это способ **легко и эффективно адаптировать LLM под новую задачу**, не трогая весь её вес.

Как это работает:

- Внутри большой LLM (сотни миллионов параметров) вместо полного переобучения весов, ты **добавляешь маленькие обучаемые вставки (матрицы)**.
    
- Они внедряются внутрь слоёв внимания.
    
- Основная модель остаётся неизменной, а учится только эта вставка.
    

📌 Пример: ты не переписываешь всю книгу, а кладёшь в неё маленькие закладки с заметками — и только их можно менять.
___
Если модель **"заморожена"**, это значит: **её параметры не обучаются**.

📌 В ICAE:

- Одна и та же LLM используется:
    
    - **как энкодер** — с LoRA-вставками (обучаема),
        
    - **как декодер** — но **в замороженном виде** (не обучаем).
        

Почему так?  
→ чтобы использовать знание LLM, но не тратить память и ресурсы на повторное обучение её целиком.
#### 🧩 **SelfCP (2024)**

- Использует LLM как компрессор
    
- Компрессия через **адаптер**
    
- Состояния агрегируются для восстановления информации
Адаптер — это **небольшой блок**, который встраивается в модель и **учится что-то специфическое**, при этом основная модель остаётся почти без изменений.

#### Пример:

- В SelfCP **адаптер используется как компрессор** — он смотрит на скрытые состояния модели и превращает их в «сжатую» форму.
    
- Потом модель использует эти состояния, чтобы продолжать работу (вопрос-ответ, генерация и т.д.).

#### 🧬 **500xCompressor (2024)**

- Автоэнкодер с:
    
    - связями между слоями
        
    - доп. задачами обучения
        
- Сжатие до **×480**
    
- Но → **качество заметно падает**
    

---

## 🔑 Сжатие KV-активаций (в кэше модели)

Цель: Уменьшить объем кэша attention’а, сохранив его эффективность

#### 🔍 **Отбор токенов**

- **Без обучения**: Zhang et al. (2023), Li et al. (2024a)
    
- **С обучением**: Qin et al. (2023, 2024)
    
- Идея: откинуть **нерелевантные токены**, оставить важные
    

#### 📚 **KV-Distill (2025)**:

- Уменьшает **KV-кэш в 100 раз**
    
- Почти **без потери качества**
    
- Обучает модель использовать компрессированные представления

## 🔹 **Сжатие подсказок (Prompt Compression)**

**Цель**: уменьшить объём текста-подсказки (prompt), чтобы снизить затраты на инференс (работу модели), при этом сохранив смысл.

### 🔧 Подходы:

- **Gist Tokens (Mu et al., 2023)**:
    
    - LLM дообучают со спец. маской.
        
    - Она сама учится сжимать подсказку в "выжимку".
        
    - Сжатие до **×26**, качество почти не падает.
        
- **LLMLingua (Jiang et al., 2023)**:
    
    - Сжатие выносится за пределы LLM.
        
    - Есть **бюджетный контроллер**, который шаг за шагом удаляет лишние токены.
        
    - Сжатие до **×20**, потери минимальны.
        
- **Jiang et al. (2024) и Pan et al. (2024)**:
    
    - Развивают LLMLingua.
        
    - Применяют её к **длинным контекстам**, используют **дистилляцию данных** для сохранения сути.
        

### 💡 Интересный вывод:

- **Morris et al. (2023, 2024)** показали: даже если дать модели только её **предсказания или эмбеддинги**, она может восстановить исходный текст.
    
- Это поднимает вопрос: **насколько можно сжать информацию, прежде чем она теряется?**
    

---

## 🔹 **Безпотерьное сжатие с помощью LLM (Lossless Compression)**

**Цель**: полностью восстановить оригинальный текст без потерь информации.

### 🔧 Подходы:

- **LLMZip (Valmeekam et al., 2023)**:
    
    - Использует вероятности, с которыми LLM выбирает следующий токен, для выбора «наиболее вероятных» кандидатов при сжатии.
        
- **FineZip (Mittu et al., 2024)**:
    
    - Дообучает модель.
        
    - Использует **динамический контроль контекста** (умная обрезка или фокусировка на важном).
        
- **Метрика**: эффективность измеряется в **битах на токен** — сколько информации нужно, чтобы закодировать текст.
    
- **Huang et al. (2024), Guo et al. (2024)**:
    
    - Сравнивают разные открытые LLM по этой метрике.
        
    - Находят связь между уровнем сжатия и качеством модели.
        
- **Наш метод**:
    
    - **Не использует внешние алгоритмы** (например, .zip).
        
    - Всё делает сама LLM — и сжимает, и восстанавливает.
        
    - Это даёт **новое понимание пределов сжатия**.
        

> 📝 **Сноска**: Delétang et al. (2024) кодируют текст в **битовые последовательности** через арифметическое кодирование. Это другая ветка, не совместимая напрямую с LLM — такие битовые данные не подать на вход модели.

---

## 🔹 **Обучаемые токены (Learned Tokens)**

**Цель**: использовать особые токены, которые учатся сами и могут менять поведение модели без изменения её весов.

### 🔧 Примеры:

- **Токены памяти** (Burtsev et al., 2021):
    
    - Дополнительные токены, хранящие важные представления (память).
        
- **Глобальные токены** (Beltagy, Zaheer, 2020):
    
    - Передают информацию на **дальние расстояния** в тексте (важно для длинных документов).
        
- **Soft Prompts**:
    
    - Исследования Li & Liang, Lester, Liu, Gao (2021–2024).
        
    - Эти токены учатся как «передавать команды» модели, **не меняя саму модель**.
        
    - Альтернатива традиционному fine-tuning.
        

### 💡 Вывод:

- Авторы утверждают, что **ёмкость представлений** (насколько много информации можно вложить в токены) задаёт **потолок эффективности** этих методов.
    

---

Если хочешь, могу превратить это объяснение в схему или mind map — текстовую или графическую.