> [!NOTE]
> Given an integer array `nums` and an integer `k`, return _the_ `kth` _largest element in the array_.
> 
> Note that it is the `kth` largest element in the sorted order, not the `kth` distinct element.
> 
> Can you solve it without sorting?
> 
> **Example 1:**
> 
> **Input:** nums = [3,2,1,5,6,4], k = 2
> **Output:** 5
> 
> **Example 2:**
> 
> **Input:** nums = [3,2,3,1,2,4,5,5,6], k = 4
> **Output:** 4
> 
> **Constraints:**
> 
> - `1 <= k <= nums.length <= 105`
> - `-104 <= nums[i] <= 104`

Решение "в тупую"
```python
class Solution(object):

	def findKthLargest(self, nums, k):
	
		a = sorted(nums)[::-1]
		
		return a[k-1]
```
### 🔧 **Идея решения без сортировки: использовать минимальную кучу (min-heap) из k элементов**

#### Шаги:

1. Создай **min-heap** (минимальную кучу) размером `k`.
    
2. Проходи по всем элементам массива `nums`.
    
3. Для каждого элемента:
    
    - Если в куче меньше `k` элементов, просто добавь его.
        
    - Иначе:
        
        - Если текущий элемент больше **наименьшего** элемента в куче (а это вершина min-heap), то:
            
            - Удали вершину (наименьший элемент).
                
            - Добавь текущий элемент.
                
4. После прохода по массиву в куче останутся `k` наибольших элементов, а на вершине будет **k-й наибольший элемент**.
    

---

### 🧠 Почему это работает?

Куча всегда хранит только **k самых больших** элементов из уже просмотренных. Самый маленький из них — это и есть **k-й наибольший** элемент.

---
```python
import heapq

def findKthLargest(nums, k):
    heap = []

    for num in nums:
        heapq.heappush(heap, num)
        if len(heap) > k:
            heapq.heappop(heap)

    return heap[0]

```
### ⏱️ Временная сложность:

- Построение и поддержание кучи: **O(n log k)**, где `n` — длина массива.
    
- Это **значительно быстрее**, чем полная сортировка `O(n log n)`, особенно если `k` мало по сравнению с `n`.


Разберёмся, **почему сложность решения с min-heap — это `O(n log k)`**, а не `O(n log n)`:

---

## 📌 Что происходит в алгоритме?

Ты проходишь по всем `n` элементам массива `nums`. Для каждого элемента:

1. Добавляешь его в **min-heap** (размер кучи максимум `k`).
    
2. Если размер кучи становится больше `k`, ты удаляешь минимальный элемент.
    

---

## 🔢 Сколько времени занимает каждая операция?

- **Вставка в кучу (`heappush`)**: `O(log k)` — потому что куча содержит максимум `k` элементов.
    
- **Удаление из кучи (`heappop`)**: тоже `O(log k)` — по той же причине.
    

👉 Операции вставки и удаления выполняются по мере необходимости, но **всего — не более `n` раз**.

---

## 📈 Суммарная сложность

Ты выполняешь `n` вставок в кучу, и иногда — удаление.

- Каждая вставка/удаление стоит `O(log k)`.
    
- Всего `n` таких операций.
    

**Итог:**

O(n⋅log⁡k)O(n⋅logk)​

---

## 🔍 Сравнение с сортировкой:

- Сортировка: `O(n log n)` — всегда.
    
- Heap-метод: `O(n log k)` — особенно выгодно, если `k` маленькое.
    

Например:

- Если `n = 100000`, `k = 10`:  
    `log n ≈ 17`, `log k ≈ 3`,  
    → сортировка ~ в **5–6 раз медленнее**.