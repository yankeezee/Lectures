
---
> [!problem]
> You are given two **non-empty** linked lists representing two non-negative integers. The digits are stored in **reverse order**, and each of their nodes contains a single digit. Add the two numbers and return the sum as a linked list.
> 
> You may assume the two numbers do not contain any leading zero, except the number 0 itself.
> 
> **Example 1:**
> 
> ![](https://assets.leetcode.com/uploads/2020/10/02/addtwonumber1.jpg)
> 
> **Input:** l1 = [2,4,3], l2 = [5,6,4]
> **Output:** [7,0,8]
> **Explanation:** 342 + 465 = 807.
> 
> **Example 2:**
> 
> **Input:** l1 = [0], l2 = [0]
> **Output:** [0]
> 
> **Example 3:**
> 
> **Input:** l1 = [9,9,9,9,9,9,9], l2 = [9,9,9,9]
> **Output:** [8,9,9,9,0,0,0,1]
> 
> **Constraints:**
> 
> - The number of nodes in each linked list is in the range `[1, 100]`.
> - `0 <= Node.val <= 9`
> - It is guaranteed that the list represents a number that does not have leading zeros.
> 

---
### 🧠 **Идея**

Два числа хранятся в виде связанных списков, где каждый узел — это одна цифра числа, и **цифры расположены в обратном порядке** (единицы в начале). Нужно сложить эти два числа и вернуть сумму также в виде связанного списка в обратном порядке.
### ✅ **Алгоритм пошагово**

1. Создаем **пустой "результирующий список"**.
    
2. Вводим **переменную `carry`** (перенос в следующий разряд) и изначально ставим `carry = 0`.
    
3. Проходим по обоим спискам **пока не дойдем до конца обоих** и пока есть перенос `carry`:
    
    - Берем значение текущего узла из `l1` (или 0, если конец).
        
    - Берем значение текущего узла из `l2` (или 0, если конец).
        
    - Складываем их с учетом `carry`.
        
    - Записываем **остаток от деления суммы на 10** в новый узел (это текущая цифра).
        
    - Перенос `carry = сумма // 10`.
        
4. В конце, если после всех сложений остался перенос (`carry > 0`), создаем узел с этим значением.
```python
# Definition for singly-linked list.
# class ListNode(object):
# def __init__(self, val=0, next=None):
# self.val = val
# self.next = next

class Solution(object):
	def addTwoNumbers(self, l1, l2):
	
		dummy_head = ListNode(0)
		current = dummy_head
		carry = 0
		
		while l1 or l2 or carry:
		
			val1 = l1.val if l1 else 0
			val2 = l2.val if l2 else 0
			
			total = val1 + val2 + carry
			carry = total // 10
			current.next = ListNode(total%10)
			current = current.next
			
			if l1:
			l1 = l1.next
			
			if l2:
			l2 = l2.next
		
		return dummy_head.next
```
## 🕒 Временная сложность (Time Complexity): **O(N)**

### Почему?

- Мы проходим по каждому узлу обоих списков ровно один раз.
    
- На каждой итерации выполняем **фиксированное количество операций** (сложение, деление, создание нового узла и переход).
    
- Даже если один список длиннее другого, мы просто продолжаем использовать 0 вместо значения.
    

➡️ **Итого:**  
Время пропорционально длине самого длинного списка:  
**`O(max(n, m)) = O(N)`**
## 💾 Пространственная сложность (Space Complexity): **O(N)**

### Почему?

- Мы создаём **новый связанный список**, длина которого:
    
    - В худшем случае = `max(n, m) + 1` (если есть перенос `carry` на самый последний разряд).
        
- Используем только **постоянное количество переменных** (`val1`, `val2`, `carry`, `current`, и т.д.).
    

➡️ **Итого:**  
Память, затрачиваемая под результат — тоже **`O(N)`**
