> [!problem]
> For two strings `s` and `t`, we say "`t` divides `s`" if and only if `s = t + t + t + ... + t + t` (i.e., `t` is concatenated with itself one or more times).
> 
> Given two strings `str1` and `str2`, return _the largest string_ `x` _such that_ `x` _divides both_ `str1` _and_ `str2`.
> 
> **Example 1:**
> 
> **Input:** str1 = "ABCABC", str2 = "ABC"
> **Output:** "ABC"
> 
> **Example 2:**
> 
> **Input:** str1 = "ABABAB", str2 = "ABAB"
> **Output:** "AB"
> 
> **Example 3:**
> 
> **Input:** str1 = "LEET", str2 = "CODE"
> **Output:** ""
> 
> **Constraints:**
> 
> - `1 <= str1.length, str2.length <= 1000`
> - `str1` and `str2` consist of English uppercase letters.
### 💡 Идея решения:

Чтобы найти **наибольшую строку `x`**, которая делит и `str1`, и `str2`, нужно найти **общий делитель строк** — такую строку, которая при многократной конкатенации образует и `str1`, и `str2`.
### 📌 Наблюдение:

Если существует такой делитель `x`, то:

- `str1 + str2 == str2 + str1`  
    (если строки состоят из одного и того же "блока", то при перестановке они равны)
    
И тогда:

- Ответом будет:  
    `str1[:gcd(len(str1), len(str2))]`  
    (т.к. длина делителя должна делить обе строки полностью)

```python
class Solution(object):

	def gcdOfStrings(self, str1, str2):
	
		def gcd(a, b):
		
			while b:
			
				a, b = b, a % b
			
			return a
	
	if str1 + str2 != str2 + str1:
	
		return ""
	
	length = gcd(len(str1), len(str2))
	
	return str1[:length]
```

## ⏱ Оценка сложности

- **Время:** `O(n + m)` — на сравнение строк и извлечение подстроки.
    
- **Память:** `O(n + m)` — на временные строки при сложении.

 **почему оценка сложности решения — `O(n + m)`**, где:

- `n = len(str1)`
    
- `m = len(str2)`
    

---

## 📦 1. Проверка `str1 + str2 == str2 + str1`

- Операция `+` создаёт новые строки длиной `n + m`
    
- Сравнение двух строк длиной `n + m` — тоже `O(n + m)`
    

### ⏱ Время: `O(n + m)`

### 🧠 Память: `O(n + m)` — создаются временные строки при сложении

---

## 📐 2. Вычисление НОД: `gcd(n, m)`

- Это делается через алгоритм Евклида, работает за `O(log(min(n, m)))`
    

### ⏱ Время: `O(log(min(n, m)))` — пренебрегаем, т.к. это мало по сравнению с первым шагом

---

## ✂️ 3. Извлечение подстроки: `str1[:gcd_len]`

- Это `O(k)`, где `k = gcd(n, m)` — но в худшем случае `k ≈ min(n, m)`
    
- Это ≤ `O(n + m)`
    

---

## ✅ Итог

|Этап|Время|Память|
|---|---|---|
|Склейка и сравнение|`O(n + m)`|`O(n + m)`|
|Вычисление GCD|`O(log(min))`|`O(1)`|
|Срез строки|`O(gcd_len)`|`O(gcd_len)`|

---