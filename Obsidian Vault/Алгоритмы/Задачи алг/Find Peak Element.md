> [!NOTE]
> A peak element is an element that is strictly greater than its neighbors.
> 
> Given a **0-indexed** integer array `nums`, find a peak element, and return its index. If the array contains multiple peaks, return the index to **any of the peaks**.
> 
> You may imagine that `nums[-1] = nums[n] = -∞`. In other words, an element is always considered to be strictly greater than a neighbor that is outside the array.
> 
> You must write an algorithm that runs in `O(log n)` time.
> 
> **Example 1:**
> 
> **Input:** nums = [1,2,3,1]
> **Output:** 2
> **Explanation:** 3 is a peak element and your function should return the index number 2.
> 
> **Example 2:**
> 
> **Input:** nums = [1,2,1,3,5,6,4]
> **Output:** 5
> **Explanation:** Your function can return either index number 1 where the peak element is 2, or index number 5 where the peak element is 6.
> 
> **Constraints:**
> 
> - `1 <= nums.length <= 1000`
> - `-231 <= nums[i] <= 231 - 1`
> - `nums[i] != nums[i + 1]` for all valid `i`.
## ❓ Что ищем

Нам нужно найти **любой пик** в массиве `nums`. Элемент считается пиковым, если он **строго больше** своих соседей:

> `nums[i] > nums[i - 1]` и `nums[i] > nums[i + 1]`

*Гарантируется, что хотя бы один пик в массиве есть.*

---

## 🔍 Почему можно использовать бинарный поиск

В обычных задачах бинарного поиска мы ищем конкретное значение в **монотонной** (упорядоченной) структуре. Но тут массив может быть как угодно устроен. Почему всё же работает?

Потому что:

* если `nums[mid] > nums[mid + 1]`, то **направо точно не имеет смысла идти**, потому что там **убывает**. Пик может быть на `mid` или **где-то левее**.
* если `nums[mid] < nums[mid + 1]`, то массив **растёт направо**, и пик **обязательно будет справа**.

🎯 Это работает потому, что:

* если массив идёт вверх, он либо достигнет пика, либо упрётся в границу (которая условно "минус бесконечность").
* если идёт вниз, значит пик уже **слева или на месте**.

```python
class Solution:
    def findPeakElement(self, nums):
        left = 0
        right = len(nums) - 1

        while left < right:
            mid = (left + right) // 2
            if nums[mid] > nums[mid + 1]:
                right = mid
            else:
                left = mid + 1

        return left
```
---
Отличный вопрос — рассмотрим подробно, как **бинарный поиск** будет работать на примере:

```
nums = [1, 3, 1, 2, 1, 10, 1]
```

Здесь тоже **несколько пиков**: `3`, `2`, `10` — и задача алгоритма найти **любой один из них**.

---

## 🔄 Шаг за шагом

Начальные границы:

```
left = 0
right = 6
```

---

### ✅ Итерация 1:

```
mid = (0 + 6) // 2 = 3
nums[3] = 2
nums[4] = 1
→ 2 > 1 → правее спуск → правее смысла нет → идём влево
→ right = mid = 3
```

Новый диапазон:

```
left = 0
right = 3
```

---

### ✅ Итерация 2:

```
mid = (0 + 3) // 2 = 1
nums[1] = 3
nums[2] = 1
→ 3 > 1 → правее спуск → идём влево
→ right = mid = 1
```

Новый диапазон:

```
left = 0
right = 1
```

---

### ✅ Итерация 3:

```
mid = (0 + 1) // 2 = 0
nums[0] = 1
nums[1] = 3
→ 1 < 3 → правее подъём → идём вправо
→ left = mid + 1 = 1
```

---

### 🏁 Завершение:

```
left = right = 1
```

Алгоритм завершился, возвращает индекс `1`, где `nums[1] = 3` — это один из пиков.

---

## 🔍 Почему он не нашёл 10?

Потому что **бинарный поиск не ищет глобальный максимум**, он ищет **любой локальный пик**.

На каждом шаге:

* Если видит спуск — идёт влево.
* Если видит подъём — идёт вправо.

Но **всегда сужает диапазон вдвое** и **всегда находит пик**, потому что:

* пики **гарантированно существуют** (за счёт граничных условий и спусков/подъёмов),
* и алгоритм **никогда не идёт в "пустую" сторону**.

---

## 📌 Вывод

Алгоритм на `nums = [1, 3, 1, 2, 1, 10, 1]`:

* нашёл пик `3` на позиции `1`;
* **не нашёл `10`**, потому что **не обязан находить наибольший пик**;
* **работает правильно**, так как гарантирует **нахождение любого пика за O(log n)**.

## 🧠 Почему сложность O(log n)

Каждый шаг **делит текущий диапазон пополам**, т.е. уменьшает его в 2 раза:

```python
mid = (left + right) // 2
```

Затем либо `left = mid + 1`, либо `right = mid`. То есть диапазон `[left, right]` сокращается.

Количество таких делений логарифмическое:

$$
\log_2 n
$$

Именно столько раз нужно, чтобы диапазон размера `n` сократился до одной точки. Поэтому:

> **Время работы — O(log n)**.

---

## 🧾 Почему память O(1)

Алгоритм использует **только переменные `left`, `right`, `mid`**, без рекурсии и без дополнительной памяти:

> **Память — O(1)**.

---

## 💡 Интуитивно:

Массив можно представить как горный ландшафт. Где бы ты ни оказался — смотри на **соседа справа**:

* Если там выше — иди вправо (вверх по склону).
* Если ниже — иди влево (к пику или вниз).

Ты **всегда приближаешься к локальному максимуму**, и за логарифмическое количество шагов обязательно его найдёшь.

---

Хочешь, могу нарисовать на примере или смоделировать это в коде.
