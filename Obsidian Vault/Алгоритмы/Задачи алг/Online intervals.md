> [!NOTE]
> Даны два списка интервалов, когда пользователь A и пользователь B были онлайн. Нужно найти все интервалы, когда оба были онлайн одновременно.
> 
  
```python
def intersect_intervals(a, b):

    i = j = 0
    result = []

    while i < len(a) and j < len(b):

        a_start, a_end = a[i]

        b_start, b_end = b[j]

  

        start = max(a_start, b_start)

        end = min(a_end, b_end)

  

        if start < end:

            result.append((start, end))

  

        if a_end < b_end:

            i += 1

        else:

            j += 1

    return result
```

## 📌 Задача

У тебя есть **два списка интервалов времени**, когда пользователь A и пользователь B были онлайн:

`a = [(1, 5), (10, 14), (16, 18)] 
`b = [(2, 6), (8, 11), (15, 17)]`

Нужно найти **все отрезки времени, когда оба пользователя были онлайн одновременно**.

---

## 🧠 Идея

Каждый список отсортирован по началу интервала.

Пусть у тебя есть текущие интервалы:

- от пользователя A: `(a_start, a_end)`
    
- от пользователя B: `(b_start, b_end)`
    

👉 Пересечение этих интервалов существует, если:

`max(a_start, b_start) < min(a_end, b_end)`

Тогда они пересекаются на отрезке:

`(start, end) = (max(a_start, b_start), min(a_end, b_end))`

---

## 🔁 Алгоритм с двумя указателями

`i = j = 0 while i < len(a) and j < len(b):`

- `i` — индекс текущего интервала из `a`
    
- `j` — индекс текущего интервала из `b`
    

### Внутри цикла:

1. **Находим пересечение** (если оно есть):
    
    `start = max(a_start, b_start) end = min(a_end, b_end)  if start < end:     result.append((start, end))`
    
2. **Двигаем тот указатель, чей интервал закончится раньше:**
    
    `if a_end < b_end:     i += 1 else:     j += 1`
    

📌 Это ключевой момент!  
Мы не можем двигать оба указателя, потому что можем пропустить потенциальные пересечения.

---

## 🧪 Пример

Пусть:

`a = [(1, 5), (10, 14), (16, 18)] b = [(2, 6), (8, 11), (15, 17)]`

Процесс шаг за шагом:

|`a[i]`|`b[j]`|Пересечение|Результат|Куда идём?|
|---|---|---|---|---|
|(1, 5)|(2, 6)|(2, 5)|✅ добавим|i += 1|
|(10, 14)|(2, 6)|✖||j += 1|
|(10, 14)|(8, 11)|(10, 11)|✅ добавим|j += 1|
|(10, 14)|(15, 17)|✖||i += 1|
|(16, 18)|(15, 17)|(16, 17)|✅ добавим|j += 1|

🔚 Конец: `j == len(b)` → выходим

✅ **Ответ:**

python

КопироватьРедактировать

`[(2, 5), (10, 11), (16, 17)]`

---

## ⏱ Сложность

- **Время:** O(n + m), где `n = len(a)`, `m = len(b)`
    
- **Память:** O(1) дополнительной памяти (результат не в счёт)
    

---